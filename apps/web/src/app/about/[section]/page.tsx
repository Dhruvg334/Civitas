import { Nav } from "@/components/site";
const pages:Record<string,[string,string]>={app:['About Civitas','Civitas helps municipalities turn fragmented public reports into structured, evidence-backed operational decisions.'],why:['Why it is needed','Reports can be incomplete, duplicated or routed without enough context. Civitas keeps evidence, priority, policy grounding and human decisions connected.'],developers:['About developers','Civitas is built as a modular civic intelligence system spanning backend operations, geospatial analysis, ML, knowledge grounding and workflow orchestration.']};
export default async function About({params}:{params:Promise<{section:string}>}){const {section}=await params;const [title,text]=pages[section]??pages.app;return <><Nav/><main className="prose"><p className="eyebrow">About Civitas</p><h1>{title}</h1><p className="lede">{text}</p></main></>}
