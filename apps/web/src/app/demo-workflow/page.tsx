"use client"; import { useState } from "react"; import { Nav } from "@/components/site";
const stages=[['Reports','Three residents report water near a school crossing.'],['Incident','Related reports are grouped into one operational incident.'],['Assessment','Severity and priority remain distinct; school context changes urgency.'],['Grounding','Water and traffic playbook evidence supports the recommendation.'],['Review','A reviewer approves the work order before citizen communication.']];
export default function Demo(){const [step,setStep]=useState(0);return <><Nav/><main className="demo"><p className="eyebrow">Seeded water-leak scenario</p><h1>Follow one report to a responsible decision.</h1><div className="timeline">{stages.map(([name],i)=><button key={name} onClick={()=>setStep(i)} className={i===step?'active':''}>0{i+1} {name}</button>)}</div><section className="stage"><span>Stage {step+1}</span><h2>{stages[step][0]}</h2><p>{stages[step][1]}</p></section></main></>}
