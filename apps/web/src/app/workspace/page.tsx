import Link from "next/link";
import { Nav, Status } from "@/components/site";
const rows = [["Water leak near school", "Water leakage", "High", "Waiting for review"], ["Blocked pathway", "Fallen tree", "Medium", "Running"], ["Streetlight outage", "Streetlight", "High", "Waiting for clarification"]];
export default function Workspace() { return <><Nav /><main className="workspace"><header><div><p className="eyebrow">Municipal workspace</p><h1>Incident queue</h1></div><button className="button small">Filter queue</button></header><div className="queue"><div className="queuehead"><span>Incident</span><span>Category</span><span>Priority</span><span>Workflow</span></div>{rows.map((row) => <Link className="qrow" href="/incidents/demo-water" key={row[0]}><b>{row[0]}</b><span>{row[1]}</span><span>{row[2]}</span><Status>{row[3]}</Status></Link>)}</div><section className="map"><p className="eyebrow">Spatial context</p><h2>Incident locations and nearby landmarks</h2><div className="mapdots"><i /><i /><i /></div><p>Map data appears when connected to the Civitas geospatial API. This workspace uses the seeded demonstration incident.</p></section></main></>; }
