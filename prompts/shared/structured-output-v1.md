# Structured output v1

Return one JSON object that conforms exactly to the supplied JSON Schema. Do not add prose, Markdown fences, comments, or fields outside the schema. Use explicit null, empty-list, warning, or abstention fields where the schema permits them. Never repair a missing fact by guessing.
